pipeline:
  identifier: Build_jhttp_1733159980115
  name: Build jhttp
  orgIdentifier: default
  projectIdentifier: RichLiuHarnessLab
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: Rich
        repoName: jhttp
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
            paths: []
          cloneCodebase: true
          execution:
            steps:
              - step:
                  type: GitClone
                  name: clone_jhttp
                  identifier: clone_jhttp
                  spec:
                    connectorRef: Rich
                    repoName: jhttp
                    build:
                      type: branch
                      spec:
                        branch: main
              - step:
                  type: Run
                  name: maven
                  identifier: buildpush
                  spec:
                    connectorRef: realfiction
                    image: harness-java-app
                    shell: Bash
                    command: |-
                      mvn clean install
                      mvn test
              - step:
                  type: Run
                  name: buildpushmaybe
                  identifier: buildpushmaybe
                  spec:
                    connectorRef: realfiction
                    image: harness-java-app
                    shell: Bash
                    command: |-
                      docker build -t realfiction/harness-java-app:latest .
                      docker push realfiction/harness-java-app:latest
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: Minikube_Connector
              namespace: harness-java-app
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          sharedPaths:
            - github
          buildIntelligence:
            enabled: false
        type: CI
        description: ""
